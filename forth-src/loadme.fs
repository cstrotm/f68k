\ utilities loadscreen                                           JPS 04-21-92
cr
.( including ON-LINE-DOC ) include on-line-doc          cr
.( including :DOES> )   include baden's-:DOES>          cr
\ .( including LIST> )  include paysan's-LIST>          cr
.( including DISASSEMBLER)        include DISASSembler            cr

cd float
.( including FLOAT )    include loadfloat   cr     \ needs CASE
cd ..

.( including VALUEs )   include ANSI/ANS-values         cr  \ e.g. FVALUE
.( including LOCALs )   include ANSI/ANS-locals_(to)    cr  \ e.g. FLOCAL
.( including AUTOLOAD ) include AUTO                    cr
.( including UCS )      include UCS                     cr
